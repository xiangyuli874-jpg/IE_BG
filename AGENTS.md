# 项目说明：工时测量与负荷山积模板

## 人机作业分析模板

- 推荐文件：`outputs/man_machine_analysis/人机作业分析自动排程模板_v1.xlsm`
- 构建脚本：`tools/man_machine_analysis/build_template.ps1`
- 验证脚本：`tools/man_machine_analysis/verify_template.ps1`
- VBA 源码：`tools/man_machine_analysis/vba/`
- 上限：5 人、10 台设备、每台 20 步。
- 颜色：黄色必填、浅蓝选填、灰色自动输出。
- 核心指标：循环周期、平均节拍、小时产能、人员负荷率、设备利用与等待、移动时间。
- 修改 VBA、工作表结构、按钮、图表或指标后，必须重新运行构建和验证脚本。
- 新版本不得覆盖工时测量 v16 或动作要素 v3 模板。

## 项目背景

本项目用于整理和生成一套 Excel 工时测量、标准工时计算、工序负荷山积图分析模板。

最初参考文件是别人制作的带公式工作簿：

- `工时测量表+员工负荷山积表.xlsx`

后续基于该文件的逻辑，重新构建了一个更便于维护和扩展的新模板。当前推荐使用的最新版文件是：

- `outputs/worktime_new_template/工时测量与负荷山积自动扩展模板_v16.xlsx`

项目当前包含两部分：

- Excel 工时测量、标准工时计算、工序负荷山积图模板。
- `移动网页/` 下的 Vite + React 在线工时测量工具，可部署到 Netlify 或 Vercel，用于现场录入、保存测量单并导出 Excel。

主要资产是 Excel 文件、移动网页子项目代码和少量生成/验证脚本。

## 当前目录结构

- `工时测量表+员工负荷山积表.xlsx`
  - 原始参考文件，不建议覆盖或删除。
  - 用于理解原作者的工时测量、标准工时、评比系数、宽放率和山积图逻辑。

- `outputs/worktime_new_template/工时测量与负荷山积自动扩展模板_v16.xlsx`
  - 当前推荐模板。
  - 基于 v15 另存生成，未直接修改 v15。
  - `工序测量` 已删除 `作业要素` 列。
  - `工序测量` 页面不显示说明行，规则说明已移动到 `使用说明`。
  - 新增 `简洁版工时表` 工作表，只显示 `工序号`、`工序名称`、`平均实测(s)`、`标准工时(s)` 四列，并通过公式从 `tblProcess` 自动抓取。
  - `工序号` 使用表格公式自动编号，新增行会自动连续生成序号。
  - `人员` 使用表格公式默认自动填充为 `1`，位置在 `测量5` 与 `平均实测(s)` 之间。
  - `状态` 改为按 `负荷ST(s)/TT基准(s)` 的 3 档判断：`负荷低`、`正常`、`超节拍`。
  - `状态` 后新增 `最大CT(s)` 和 `波动风险`，波动风险按 `最大CT(s)/TT基准(s)` 判断为 `高`、`中`、`低`。
  - `平均实测(s)`、`标准工时(s)`、`负荷ST(s)` 均设置为数字格式并保留一位小数。
  - `TT基准(s)`、`最大CT(s)` 仍保留两位小数。
  - 山积图会随 `工序测量` 表格实际行数自动扩展，避免固定 140 行导致后方大面积空白。
  - 山积图中 `负荷ST(s) > TT基准(s)` 的工序显示红色，其余显示蓝色。
  - 山积图柱形数据标签默认显示一位小数，并隐藏辅助系列的零值标签。
  - `山积分析` 左上方已升级为指标小表格，包含 `总工时`、`线平衡`、`瓶颈时间`、`TT时间`。
  - 工作簿主要文字与图表文字统一为微软雅黑。
  - 后续如需继续调整，应优先基于此文件。

说明：`worktime_new_template` 目录中的 v5-v15 文件已按用户要求删除，目前只保留最新的 v16。

- `outputs/worktime_logic_test/工时测量表_山积逻辑测试版.xlsx`
  - 中间逻辑测试文件。
  - 主要用于验证早期公式、山积图和表格逻辑，不是最终交付版本。

- `outputs/worktime_action_element_template/动作要素工时与堆叠山积图模板_v3.xlsm`
  - 当前动作要素级推荐模板，宏启用版本。
  - 用于录入一个工序下多个动作要素的 ST1-ST5 测量值。
  - 动作序号按 `ST1` 到 `ST5` 显示。
  - 山积图以每个工序为一根柱，按 `ST1` 到 `ST5` 堆叠显示真实动作平均ST，并带时间标签。
  - 另有 `员工负荷表`，横向自动抓取工序、动作、时间、总工时、目标节拍、实际节拍，并在底部显示堆叠图。
  - 默认 25 个工序，每个工序 5 个动作，共 125 行。
  - 通过宏按钮支持中间新增/删除工序，也支持在表格底部输入新工序时自动扩展 5 行。

- `.codex_tmp/`
  - Codex 生成和验证 Excel 时使用的临时脚本与检查文件。
  - 其中 `create_new_template_xlsm.ps1` 是生成当前模板的主要脚本。
  - 该目录不是用户最终使用入口，但对后续复现模板生成过程有参考价值。

- `移动网页/`
  - 在线工时测量工具独立子项目。
  - 根目录中原先的在线工具源码已迁移到该目录。
  - 该目录包含自己的 `package.json`、`netlify.toml`、`src/`、`netlify/`、`scripts/` 和 `outputs/`。
  - `移动网页/outputs/worktime_new_template/工时测量与负荷山积自动扩展模板_v16.xlsx` 是在线导出接口使用的模板副本。

- `移动网页/src/`
  - 在线工时测量工具前端源码。
  - 当前使用 Vite、React、TypeScript。
  - `移动网页/src/data/presets.ts` 维护线体、机型、班组和默认工序预设。
  - 当前启用 `A线`，支持 `普通单洗`、`DD单洗`、`普通洗烘一体机`、`DD洗烘一体机`。
  - 当前界面已针对移动端窄屏录入做过优化，包括状态栏、操作菜单、工序横向录入行和底部导出栏。
  - `mobileKeyboard.ts` 监听移动键盘和 Visual Viewport 状态，避免固定导出栏遮挡录入区域。
  - `deploymentMode.ts` 通过 `VITE_REQUIRE_ACCESS_CODE` 控制前端是否显示访问码入口。

- `移动网页/netlify/functions/`
  - 在线工具后端接口。
  - `access.ts` 校验访问码。
  - `sessions.ts` 创建、读取、保存测量单。
  - `export.ts` 按测量单导出 Excel。
  - `_shared/session.ts` 使用 Netlify Blobs 保存测量单。
  - 导出接口复用 `移动网页/shared/excel.ts` 生成工作簿。

- `移动网页/api/`
  - Vercel Functions 接口。
  - `api/sessions/index.ts` 创建测量单。
  - `api/sessions/[id].ts` 读取和保存测量单。
  - `api/export.ts` 导出当前班组或整机 Excel。
  - `api/_lib/sessionRepository.ts` 使用 Upstash Redis 保存测量单，并支持从旧 Netlify 站点按需迁移。
  - `api/_lib/rateLimit.ts` 对创建、读取、保存和导出接口实施独立的滑动窗口限流。

- `移动网页/shared/`
  - Netlify 与 Vercel 共用逻辑。
  - `sessionCore.ts` 负责测量单创建、校验和规范化。
  - `excel.ts` 负责生成 Excel 导出文件。

- `移动网页/scripts/verify-export.ts`
  - 导出逻辑验证脚本。
  - 会生成 `移动网页/.codex_tmp/verify_export.xlsx`，该文件属于验证产物，不应提交。

- `移动网页/netlify.toml`
  - Netlify 构建配置。
  - 构建命令为 `npm run build`，发布目录为 `dist`。
  - Functions included files 包含 v16 Excel 模板。
  - 如果从仓库根目录连接 Netlify，站点 Base directory 应设置为 `移动网页`。

- `移动网页/vercel.json`
  - Vercel 独立部署配置。
  - Vite 构建输出目录为 `dist`。
  - `/api/*` 保留为 Vercel Functions，其余路径回退到 `index.html`。
  - `api/export.ts` 会包含 v16 Excel 模板，并将最大执行时间设为 60 秒。

- `移动网页/package.json`
  - 在线工具项目配置。
  - 常用命令：
    - `npm run dev`
    - `npm test`
    - `npm run build`
    - `npm run verify:export`

## 在线工具说明

在线工具当前能力：

- 通过 `SITE_ACCESS_CODE` 环境变量配置访问码。
- 前端请求使用 `X-Access-Code` 请求头传递访问码。
- 支持新建测量单、读取测量单、自动保存测量单。
- 测量单保存到 Netlify Blobs 的 `worktime-sessions` store。
- 生产环境使用 site-scoped store，非生产部署使用 deploy-scoped store。
- 支持复制带 `session` 参数的测量单链接。
- 支持导出当前班组或整机 Excel。
- 导出文件会带现场测量记录，并按实际工序数扩展 `tblProcess` 和山积图范围。
- Netlify 版使用 Netlify Blobs 保存测量单，并通过 `SITE_ACCESS_CODE` 保护接口。
- Vercel 版使用 Upstash Redis 保存测量单，并通过 `VITE_REQUIRE_ACCESS_CODE=false` 关闭前端访问码入口。
- Vercel Redis 环境变量优先使用 `UPSTASH_REDIS_REST_URL`、`UPSTASH_REDIS_REST_TOKEN`，也兼容 `KV_REST_API_URL`、`KV_REST_API_TOKEN`。
- 可选的 `NETLIFY_MIGRATION_BASE_URL` 和 `NETLIFY_MIGRATION_ACCESS_CODE` 用于在首次读取时迁移旧 Netlify 测量单。
- Vercel 接口限流策略：
  - 创建：每 IP 每小时 30 次。
  - 读取：每 IP 每 10 分钟 300 次。
  - 保存：每 IP 每 10 分钟 600 次。
  - 导出：每 IP 每小时 60 次。

本地或部署前验证建议：

1. 修改前端、共享逻辑或 Functions 后在 `移动网页/` 下运行 `npm test`。
2. 运行 `npm run build` 验证 TypeScript 和生产构建。
3. 修改导出逻辑、模板引用或预设工序后运行 `npm run verify:export`。
4. `.codex_tmp/` 和 `移动网页/.codex_tmp/` 下的验证工作簿、日志和缓存都是验证产物，不应提交。

## 原始参考表的关键逻辑

原始工作簿包含以下主要工作表：

- `工时测量记录表`
  - 记录每个作业动作的多次实测时间。
  - 计算平均值、标准差、剔除异常后的平均时间等。

- `标准工时表`
  - 基于实测时间、评比系数、宽放率、人员数、批量等参数计算标准工时。
  - 动作级标准时间汇总到工序或岗位负荷。

- `平衡率及动作变异分析图`
  - 将各工序或岗位的标准工时展开为图表辅助数据。
  - 用于生成负荷山积图、线平衡相关分析和动作变异分析。

- `评比系数`
  - 原始表中评比系数不是单一固定值，而是由多个维度综合计算。

- `宽放率`
  - 用于标准工时计算中的宽放修正。

## 当前最终模板说明

最新版模板包含以下工作表：

- `使用说明`
  - 给用户说明模板用途和基本填写方式。
  - v11 起包含状态与波动风险规则说明，v14 起状态说明为三档判断。

- `参数设置`
  - 用于设置全局参数。
  - 表头和表格内容之间没有空白行。
  - 当前参数包括：
    - `计划单班产量(pcs/班)`：默认留空，由用户自行填写。
    - `班次工时(h/班)`：默认 `11`。
    - `稼动率`：默认 `100%`。
    - `宽放率`：默认 `5%`。
    - `熟练系数`：默认 `0`。
    - `努力系数`：默认 `0`。
    - `工作环境系数`：默认 `0`。
    - `一致性系数`：默认 `0`。
    - `综合评比系数`：由上述四个评比项求和。
    - `目标节拍TT(s)`：按单班工时、稼动率、计划单班产量自动计算。

- `工序测量`
  - 用于录入工序、人员、测量值等。
  - 表头和表格内容之间没有空白行。
  - `工序号` 默认从 `1` 开始，并使用公式自动连续编号。
  - `人员` 默认自动填充为 `1`，空行保持空白；列位置在 `测量5` 和 `平均实测(s)` 之间。
  - v7 已删除 `作业要素` 列。
  - 主要计算字段包括：
    - `平均实测(s)`
    - `标准工时(s)`
    - `负荷ST(s)`
    - `TT基准(s)`
    - `状态`
    - `最大CT(s)`
    - `波动风险`
  - v8 在表格末尾增加了两个隐藏图表辅助列：
    - `负荷ST_正常显示`
    - `负荷ST_超TT显示`
  - 标准工时逻辑：
    - `标准工时 = 平均实测 * (1 + 综合评比系数) * (1 + 宽放率)`
  - 负荷 ST 用于后续山积图柱状图。
  - v14 状态逻辑：
    - `负荷ST/TT > 100%`：`超节拍`
    - `70% ~ 100%`：`正常`
    - `< 70%`：`负荷低`
  - v10 波动风险逻辑：
    - `最大CT/TT > 100%`：`高`
    - `95% ~ 100%`：`中`
    - `< 95%`：`低`

- `山积分析`
  - 包含工序负荷山积图。
  - 左上方包含指标小表格：
    - `总工时 = 标准工时总和`
    - `线平衡 = 负荷ST总和 / (最大负荷ST * 工序数量)`
    - `瓶颈时间 = 最大负荷ST`
    - `TT时间 = 参数设置中的目标节拍TT`
  - `负荷ST` 使用蓝色柱状图。
  - `超TT` 使用红色柱状图。
  - `TT基准` 使用连续折线图，不使用柱状图。
  - v6 图表随 `工序测量` 表格实际行数自动扩展，不再固定显示 140 行。
  - v15 将原单个 `线平衡率` 数据升级为四项指标小表格。
  - 线平衡率逻辑：
    - `线平衡率 = 负荷ST总和 / (最大负荷ST * 工序数量)`

- `简洁版工时表`
  - v15 新增。
  - 只展示 `工序号`、`工序名称`、`平均实测(s)`、`标准工时(s)` 四列。
  - 数据通过公式从 `工序测量` 的 `tblProcess` 自动抓取，不需要用户手动填写。
  - 样式为白底黑字、微软雅黑字体，`平均实测(s)`、`标准工时(s)` 保留一位小数。

## 动作要素堆叠山积图模板说明

动作要素模板当前推荐文件为：

- `outputs/worktime_action_element_template/动作要素工时与堆叠山积图模板_v3.xlsm`

该模板是独立 `.xlsm`，使用 VBA 宏维护新增/删除工序、报表刷新和底部自动扩展。

主要工作表：

- `使用说明`
- `参数设置`
- `动作要素测量`
- `员工负荷表`
- `山积分析`

核心逻辑：

- 每个工序预留 5 行动作要素。
- 动作序号列预置为 `ST1`、`ST2`、`ST3`、`ST4`、`ST5`。
- 每个动作可填写 `ST1` 到 `ST5` 五次测量。
- `平均ST = ST1:ST5 非空测量值平均`。
- `工序总ST = 同一工序号下 5 个动作平均ST求和`。
- `宽放ST = 工序总ST * (1 + 宽放率)`。
- 山积图柱形展示真实动作平均ST的堆叠，不展示宽放ST，动作段显示时间标签。
- TT 基准以折线叠加在堆叠柱图上。
- `员工负荷表` 按当前实际工序数横向生成，自动抓取动作名称、动作时间、总工时、目标节拍和实际节拍。
- 图表辅助区按当前实际工序数生成。
- 图表旁显示线平衡率、最大工序ST、超过TT工序数、平均工序ST。

宏版 v3 行为：

- 默认只生成 25 个工序。
- `动作要素测量` 表右侧有 `新增工序`、`删除工序`、`刷新报表` 三个按钮。
- 选中某工序任意一行后点 `新增工序`，会在该工序后插入一组 5 行。
- 选中某工序任意一行后点 `删除工序`，会删除该工序的 5 行，并弹窗确认。
- 在当前最后一个工序下方第一行填写工序名称、动作描述或测量时间，会自动扩展下一组 5 行。
- 每次新增、删除、底部扩展或刷新后，员工负荷表和山积分析图都会按当前实际填写到的最后一个工序重建，避免 `#REF!`。
- 实际填写工序按用户输入内容判断：工序名称、动作单元描述、ST1-ST5 测量值、备注任意非空即可；系统自动生成的工序号、动作序号、公式不计入。
- 若中间有空白工序但后面还有已填写工序，员工负荷表和山积图会保留中间空位。
- 山积分析图的动作段时间标签使用微软雅黑、10号、加粗、白色；零值标签通过数字格式隐藏，不显示 `0.0`。
- 员工负荷表中工序名称保持竖版，`ST1动作` 到 `ST5动作` 使用横向自动换行。

## 已处理过的重要问题

- 修正了山积图中 `负荷ST` 柱状图过细的问题。
- 将 `TT基准` 改为折线图。
- 将 `计划日产量` 改为 `计划单班产量`。
- 参数默认值调整为：
  - 班次工时：`11h/班`
  - 稼动率：`100%`
  - 宽放率：`5%`
  - 各评比系数：`0`
- `计划单班产量` 默认留空，避免误导用户。
- 修复参数说明列曾出现 `#NAME?` 的问题。
- 修正工序号从 `10` 开始的问题，改为从 `1` 开始。
- 将 `动作说明` 表头改为 `作业要素`。
- 修复工序超过 `15` 个后山积图不更新的问题，v5 按 `140` 个工序范围设计。
- v6 将山积图数据源改为跟随 `tblProcess` 表格实际行数扩展，解决图表后方固定空白过大的问题。
- v6 将 `线平衡率` 移动到 `山积分析!A3:B3`，设置黄色底色、微软雅黑、加粗。
- v7 删除 `工序测量` 中的 `作业要素` 列。
- v7 将 `工序号` 改为公式 `=ROW()-ROW(tblProcess[#Headers])`，表格新增行时自动生成连续序号。
- v8 将山积图柱形拆为蓝色 `负荷ST` 和红色 `超TT` 两个系列，超过 `TT基准` 的工序自动显示红色。
- v8 使用两个隐藏辅助列驱动红蓝柱显示，并将图表设置为允许使用隐藏列数据。
- v9 将 `人员` 列改为公式 `=IF(OR([@工序名称]<>"",COUNT([@测量1]:[@测量5])>0),1,"")`，新增行只要填写工序名称或测量值就自动显示 `1`。
- v10 将 `状态` 扩展为 6 档负荷判断，并新增 `最大CT(s)` 和 `波动风险` 两列。
- v10 波动风险不使用 P90，按当前最多 5 次测量中的最大 CT 与 TT 比较，做保守风险判断。
- v11 将 `工序测量` 页面说明移动到 `使用说明`，并将波动风险输出简化为 `高风险`、`中风险`、`低风险`。
- v12 将 `人员` 列调整到 `测量5` 和 `平均实测(s)` 之间；状态文字中 `正常负荷` 改为 `正常`，`负荷严重不足` 改为 `负荷极低`。
- v13 将波动风险输出进一步简化为 `高`、`中`、`低`，并同步更新 `使用说明`。
- v15 新增 `简洁版工时表`，统一工作簿字体为微软雅黑，并将 `山积分析` 左上方升级为 `总工时`、`线平衡`、`瓶颈时间`、`TT时间` 四项指标小表格。
- v16 将 `平均实测(s)`、`标准工时(s)`、`负荷ST(s)` 及山积图柱形数据标签调整为一位小数，并清理旧 V 版本文件，只保留最新模板。

## 后续维护建议

如果后续继续调整模板，建议遵循以下原则：

1. 优先保护原始参考表 `工时测量表+员工负荷山积表.xlsx`，不要直接覆盖。
2. 优先基于 `outputs/worktime_new_template/工时测量与负荷山积自动扩展模板_v16.xlsx` 继续迭代。
3. 修改 Excel 后，需要重点验证：
   - `参数设置` 是否有公式错误。
   - `工序测量` 新增行后公式是否自动延续。
   - `山积分析` 图表是否仍引用足够的工序范围。
   - `TT基准` 是否仍为折线图。
   - `负荷ST` 是否仍为柱状图。
   - `线平衡率` 是否随工序数据变化。
4. 如需再次生成模板，可以参考 `.codex_tmp/create_new_template_xlsm.ps1`，但应先确认脚本输出路径，避免覆盖用户正在使用的文件。
5. 修改在线工具后，需要重点验证：
   - 在 `移动网页/` 下运行 `npm test` 是否通过。
   - 在 `移动网页/` 下运行 `npm run build` 是否通过。
   - 在 `移动网页/` 下运行 `npm run verify:export` 是否通过。
   - Netlify 环境变量 `SITE_ACCESS_CODE` 是否已配置。
   - `移动网页/netlify.toml` 中 included file 是否仍指向当前最新模板。
   - Vercel 的 Upstash Redis 环境变量和 `VITE_REQUIRE_ACCESS_CODE` 是否已正确配置。
   - `移动网页/vercel.json` 中导出函数是否仍包含当前最新模板。

## 本地操作规则

禁止批量删除文件和目录。

不要使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件。

正确示例：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，并请用户手动删除。
