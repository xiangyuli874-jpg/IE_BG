# 项目说明：工时测量与负荷山积模板

## 项目背景

本项目用于整理和生成一套 Excel 工时测量、标准工时计算、工序负荷山积图分析模板。

最初参考文件是别人制作的带公式工作簿：

- `工时测量表+员工负荷山积表.xlsx`

后续基于该文件的逻辑，重新构建了一个更便于维护和扩展的新模板。当前推荐使用的最新版文件是：

- `outputs/worktime_new_template/工时测量与负荷山积自动扩展模板_v5.xlsx`

项目不是传统代码项目，主要资产是 Excel 文件和少量生成/验证用的临时脚本。

## 当前目录结构

- `工时测量表+员工负荷山积表.xlsx`
  - 原始参考文件，不建议覆盖或删除。
  - 用于理解原作者的工时测量、标准工时、评比系数、宽放率和山积图逻辑。

- `outputs/worktime_new_template/工时测量与负荷山积自动扩展模板_v5.xlsx`
  - 当前最终模板。
  - 用户目前认为该版本暂时满足要求。
  - 后续如需继续调整，应优先基于此文件。

- `outputs/worktime_logic_test/工时测量表_山积逻辑测试版.xlsx`
  - 中间逻辑测试文件。
  - 主要用于验证早期公式、山积图和表格逻辑，不是最终交付版本。

- `.codex_tmp/`
  - Codex 生成和验证 Excel 时使用的临时脚本与检查文件。
  - 其中 `create_new_template_xlsm.ps1` 是生成当前模板的主要脚本。
  - 该目录不是用户最终使用入口，但对后续复现模板生成过程有参考价值。

## 原始参考表的关键逻辑

原始工作簿包含以下主要工作表：

- `工时测量记录表`
  - 记录每个作业动作的多次实测时间。
  - 计算平均值、标准差、剔除异常后的平均时间等。

- `标准工时表`
  - 基于实测时间、评比系数、宽放率、人员数、批量等参数计算标准工时。
  - 动作级标准时间汇总到工序或岗位负荷。

- `平衡率及动作变异分析图`
  - 将各工序或岗位的标准工时展开为图表辅助数据。
  - 用于生成负荷山积图、线平衡相关分析和动作变异分析。

- `评比系数`
  - 原始表中评比系数不是单一固定值，而是由多个维度综合计算。

- `宽放率`
  - 用于标准工时计算中的宽放修正。

## 当前最终模板说明

最新版模板包含以下工作表：

- `使用说明`
  - 给用户说明模板用途和基本填写方式。

- `参数设置`
  - 用于设置全局参数。
  - 表头和表格内容之间没有空白行。
  - 当前参数包括：
    - `计划单班产量(pcs/班)`：默认留空，由用户自行填写。
    - `班次工时(h/班)`：默认 `11`。
    - `稼动率`：默认 `100%`。
    - `宽放率`：默认 `5%`。
    - `熟练系数`：默认 `0`。
    - `努力系数`：默认 `0`。
    - `工作环境系数`：默认 `0`。
    - `一致性系数`：默认 `0`。
    - `综合评比系数`：由上述四个评比项求和。
    - `目标节拍TT(s)`：按单班工时、稼动率、计划单班产量自动计算。

- `工序测量`
  - 用于录入工序、作业要素、人员、测量值等。
  - 表头和表格内容之间没有空白行。
  - `工序号` 默认从 `1` 开始。
  - 第三列表头为 `作业要素`。
  - 主要计算字段包括：
    - `平均实测(s)`
    - `标准工时(s)`
    - `负荷ST(s)`
    - `TT基准(s)`
    - `状态`
  - 标准工时逻辑：
    - `标准工时 = 平均实测 * (1 + 综合评比系数) * (1 + 宽放率)`
  - 负荷 ST 用于后续山积图柱状图。

- `山积分析`
  - 包含工序负荷山积图。
  - `负荷ST` 使用柱状图。
  - `TT基准` 使用连续折线图，不使用柱状图。
  - 图表范围已按至少 `140` 个工序设置。
  - 图表旁增加了 `线平衡率` 数据。
  - 线平衡率逻辑：
    - `线平衡率 = 负荷ST总和 / (最大负荷ST * 工序数量)`

## 已处理过的重要问题

- 修正了山积图中 `负荷ST` 柱状图过细的问题。
- 将 `TT基准` 改为折线图。
- 将 `计划日产量` 改为 `计划单班产量`。
- 参数默认值调整为：
  - 班次工时：`11h/班`
  - 稼动率：`100%`
  - 宽放率：`5%`
  - 各评比系数：`0`
- `计划单班产量` 默认留空，避免误导用户。
- 修复参数说明列曾出现 `#NAME?` 的问题。
- 修正工序号从 `10` 开始的问题，改为从 `1` 开始。
- 将 `动作说明` 表头改为 `作业要素`。
- 修复工序超过 `15` 个后山积图不更新的问题，当前按 `140` 个工序范围设计。
- 删除了最终模板所在目录中的旧版本文件，仅保留 `v5`。

## 后续维护建议

如果后续继续调整模板，建议遵循以下原则：

1. 优先保护原始参考表 `工时测量表+员工负荷山积表.xlsx`，不要直接覆盖。
2. 优先基于 `outputs/worktime_new_template/工时测量与负荷山积自动扩展模板_v5.xlsx` 继续迭代。
3. 修改 Excel 后，需要重点验证：
   - `参数设置` 是否有公式错误。
   - `工序测量` 新增行后公式是否自动延续。
   - `山积分析` 图表是否仍引用足够的工序范围。
   - `TT基准` 是否仍为折线图。
   - `负荷ST` 是否仍为柱状图。
   - `线平衡率` 是否随工序数据变化。
4. 如需再次生成模板，可以参考 `.codex_tmp/create_new_template_xlsm.ps1`，但应先确认脚本输出路径，避免覆盖用户正在使用的文件。

## 本地操作规则

禁止批量删除文件和目录。

不要使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件。

正确示例：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，并请用户手动删除。

